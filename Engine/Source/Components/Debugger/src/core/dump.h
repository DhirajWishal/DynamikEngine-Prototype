#pragma once

#ifndef _DYNAMIK_DEBUGGER_DUMP_H
#define _DYNAMIK_DEBUGGER_DUMP_H

namespace Dynamik {
	// debugger: dump a log message to the "debugger_dump.txt" file
	void dumpData(std::string msg);
}

#endif	// !_DYNAMIK_DEBUGGER_DUMP_H